% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ferobust.R
\name{frontier_audit}
\alias{frontier_audit}
\title{Plug-and-play frontier audit for a panel regression}
\usage{
frontier_audit(
  formula,
  data,
  key_var = NULL,
  psi_max = c(0, 0.3, 0.5, 0.7, 0.9, 1),
  max_lag = 5,
  reliability = c(low = 0.85, mid = 0.9, high = 0.95)
)
}
\arguments{
\item{formula}{A formula of the form \code{y ~ x + controls | unit + year}.
The first fixed effect is treated as the unit and the second as time.}

\item{data}{A data frame.}

\item{key_var}{Character name of the key regressor. Defaults to the first
regressor.}

\item{psi_max}{Numeric vector of maximum allowed one-period
measurement-error persistence values.}

\item{max_lag}{Maximum lag used in the autocorrelation frontier.}

\item{reliability}{Optional conventional reliability range still passed to
\code{\link{audit}} for the standard diagnostic table.}
}
\value{
An object of class \code{ferobust_frontier}.
}
\description{
Runs \code{\link{audit}} and adds the autocorrelation frontier for
within-reliability. This is the single-series diagnostic for point-estimate
variables when no posterior SD, repeated measure, or literature reliability is
available. The output reports a lower bound on \code{lambda_w} across
assumptions about the maximum persistence of measurement error.
}
